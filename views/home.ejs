<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meeting App | Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #333;
            color: #fff;
        }

        .container {
            max-width: 500px;
            margin-top: 100px;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            margin-bottom: 20px;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border-radius: 3px;
            background-color: #555;
            color: #fff;
            border: none;
        }

        button {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            background-color: #4caf50;
            color: #fff;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        .form-control {
            margin-bottom: 5px;
            padding: 10px;
        }
    </style>
    <script>
        function join() {
            ElementInterval = setInterval(function () {
                const x = document.getElementById("link").value;
                window.location.href = x;
                clearInterval(ElementInterval);
            }, 0);
        }
    </script>
</head>

<body>
    <div class="container">
        <h1>Meeting App</h1>
        <form action="/room">
            <button class="btn btn-primary btn-lg btn-block">New Meeting</button>
        </form>
        <form>
            <input type="text" id="link" class="form-control" placeholder="Please enter your meeting link">
            <button class="btn btn-success btn-lg btn-block" onclick="join()">Join</button>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
